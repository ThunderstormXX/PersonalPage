# PersonalPage

Static GitHub Pages site for Igor Ignashin.

## Local preview

```bash
python3 -m http.server 8000
```

Open `http://localhost:8000`.

## Deploy

The site is plain HTML/CSS in the repository root. GitHub Pages can serve it
from the `main` branch root.
